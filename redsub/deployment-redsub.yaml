apiVersion: apps/v1
kind: Deployment

metadata:
  namespace: singouins
  name: sep-backend-redis-sub
  labels:
    tier: sep-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis-sub
      tier: sep-backend
  template:
    metadata:
      annotations:
        prometheus.io/scrape: "false"
        prometheus.io/port: "5000"
      labels:
        app: redis-sub
        tier: sep-backend
    spec:

      containers:
      - name: sep-backend-redis-sub
        image: singouins/redsub:latest
        resources:
          limits:
            memory: 32Mi
            cpu: 20m
          requests:
            memory: 16Mi
            cpu: 10m
        env:
        # Reminder, Redis needs to be set with, at minimum :
        # redis-cli> CONFIG SET notify-keyspace-events s$xE
        #- name: REDSUB_PATH
        #  value: '*'
        #- name: REDSUB_QUEUE
        #  value: 'yarqueue:discord'
        # Python env vars
        - name: PYTHONUNBUFFERED
          value: 'True'
        - name: PYTHONIOENCODING
          value: 'UTF-8'
