apiVersion: v1
kind: Service

metadata:
  namespace: singouins
  name: sep-backend-api-internal-svc
  labels:
    tier: sep-backend
spec:
  selector:
    name: api-internal
    tier: sep-backend
  ports:
  - protocol: TCP
    port: 5000
