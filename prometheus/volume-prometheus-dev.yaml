apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  namespace: singouins
  name: sep-backend-prometheus
spec:
  storageClassName: csi-cinder-classic
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
status: {}
